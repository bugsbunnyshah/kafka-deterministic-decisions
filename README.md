# kafka-deterministic-decisions
End-to-end demo showing how Kafka event streams can drive deterministic, explainable decisions using a rule-driven reasoning engine (CGO).  Focuses on replay safety, fail-fast validation, stable snapshot hashing, and auditable outcomes — not chatbots or agent fluff.
